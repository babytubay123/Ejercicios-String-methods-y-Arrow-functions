/*rea una función que convierta una cadena de texto en mayúsculas usando el
método toUpperCase().*/
let = palabra = prompt("Escribe una palabra en minúsculas: ");
let convertir = (palabra) => palabra.toUpperCase();

console.log(convertir(palabra));
