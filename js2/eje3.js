/*scribe una función que convierta una cadena a minúsculas utilizando
toLowerCase().*/
let = palabra = prompt("Escribe una palabra en MAYÚSCULAS:");
let convertir = (palabra) => palabra.toLowerCase(palabra);

console.log(convertir(palabra));
